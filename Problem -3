def generate_series(x):
    series = []
    for i in range(1, x+1):
        if i % 2 != 0:
            series.append(i)
    return series

# Example usage
input_num = int(input("Enter a number: "))
result = generate_series(input_num)
print(result)
